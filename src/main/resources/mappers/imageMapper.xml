<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.board2.mapper.imageMapper">
	
	<select id="readImage" resultType="ImageVO" parameterType="int">
		SELECT date, imgFile FROM test_table WHERE date=201812
	
    </select>


</mapper>